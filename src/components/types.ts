export type Color = 'primary'|'secondary'|'success'|
'warning'|'border'|'dark'|'light'|'contrast-primary'|
'contrast-primary'|'text-primary'|'text-secondary';

export type Option = {
	label: any,
	value: any,
}